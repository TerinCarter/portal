<%= form_for(@typesoftware) do |f| %>
  <% if @typesoftware.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@typesoftware.errors.count, "error") %> prohibited this typesoftware from being saved:</h2>

      <ul>
      <% @typesoftware.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
