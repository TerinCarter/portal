<%= form_for(@semanticcategory) do |f| %>
  <% if @semanticcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@semanticcategory.errors.count, "error") %> prohibited this semanticcategory from being saved:</h2>

      <ul>
      <% @semanticcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
